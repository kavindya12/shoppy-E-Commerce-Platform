# MongoDB Atlas connection URI
MONGO_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<dbname>?retryWrites=true&w=majority

# JWT (Authentication)
JWT_SECRET=<your_jwt_secret>
JWT_EXPIRES_IN=7d

# CORS (comma-separated origins, optional)
CORS_ORIGINS=http://localhost:5173,https://yourshop.com

# Node environment
NODE_ENV=development

# Server port
PORT=5000

# Email settings (if using notifications, e.g. SendGrid, Mailgun)
EMAIL_USER=
EMAIL_PASS=
EMAIL_FROM=

# Stripe or Payment gateway API keys (for future integration)
STRIPE_SECRET_KEY=
STRIPE_PUBLISHABLE_KEY=

# --
# Notes:
# - Replace <username>, <password>, <cluster>, and <dbname> with your Atlas details
# - Never commit real secrets to version control
# - For production, update JWT_SECRET and use strong, unique values
# - Add more keys as your e-commerce platform features grow
